# Discord Botトークン（必須）
DISCORD_TOKEN=your_bot_token_here

# SQLiteデータベースの保存先
# Railway では /data/scheduler.db を推奨（Persistent Volume をマウントする場合）
# ローカル開発では scheduler.db のままでOK
DB_PATH=scheduler.db

# 開発用：特定のサーバーIDを指定するとコマンドが即時反映される（省略可）
# 本番環境では削除してグローバル同期を使う
# SYNC_GUILD_ID=123456789012345678
